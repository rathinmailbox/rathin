# Copy this file to .env and fill in your values.

# SQLite database location (relative to project root).
DATABASE_URL=file:./db/custom.db

# Admin panel password (CHANGE THIS — default is admin123).
# ADMIN_PASSWORD=your-strong-password

# Secret used to sign admin session cookies (any random long string).
# ADMIN_SECRET=any-random-long-string
